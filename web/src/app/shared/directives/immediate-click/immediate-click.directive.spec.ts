import { ImmediateClickDirective } from './immediate-click.directive';

describe('ImmediateClickDirective', () => {
  it('should create an instance', () => {
    const directive = new ImmediateClickDirective();
    expect(directive).toBeTruthy();
  });
});
